# Confetti — Slack app manifest
#
# Before pasting into https://api.slack.com/apps (Create New App → From a manifest),
# replace every `${APP_BASE_URL}` below with your real public URL
# (e.g. https://confetti.vercel.app or your ngrok tunnel for local dev).
# Slack does not interpolate env vars in this file.
display_information:
  name: Confetti
  description: Quietly runs the joyful parts of your team's culture — birthdays and anniversaries.
  background_color: "#6E40C9"
features:
  bot_user:
    display_name: Confetti
    always_online: true
  app_home:
    home_tab_enabled: true
    messages_tab_enabled: true
    messages_tab_read_only_enabled: false
  slash_commands:
    - command: /confetti
      url: https://dashboard.tryconfetti.xyz/api/slack/commands
      description: Ask Confetti to manage celebrations, settings, and your roster
      usage_hint: "[question|channel|budget|opt-outs|help]"
      should_escape: true
oauth_config:
  redirect_urls:
    - https://dashboard.tryconfetti.xyz/api/slack/oauth_callback
  scopes:
    bot:
      - chat:write
      - channels:read
      - groups:read
      - commands
      - users:read
      - users:read.email
      - im:history
      - im:write
settings:
  event_subscriptions:
    request_url: https://dashboard.tryconfetti.xyz/api/slack/events
    bot_events:
      - message.im
      - app_uninstalled
  interactivity:
    is_enabled: true
    request_url: https://dashboard.tryconfetti.xyz/api/slack/interactivity
  org_deploy_enabled: false
  socket_mode_enabled: false
  token_rotation_enabled: false